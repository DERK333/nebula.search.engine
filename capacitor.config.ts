import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nebulasearch.engine',
  appName: 'Nebula Search Engine',
  webDir: 'dist'
};

export default config;
